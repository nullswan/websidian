# Session Note

- Timestamp: 2026-04-04 21:55 UTC
- Milestone: M6 — Accounts, API, and Automation
- Task chosen: Authentication, session management, REST API documentation
- Why this task: M6 exit criteria require auth, per-user vault access, and documented API
- Changes made:
  - **Authentication**: Added `@fastify/cookie`, `@fastify/session`, `bcryptjs`. Created `auth.ts` with register/login/logout/me endpoints. `requireAuth` middleware protects vault routes. Users stored in JSON file under `DATA_DIR`.
  - **Login page**: Dark-themed login/register form with toggle. Auth check on mount (`/api/auth/me`). Logout command in command palette.
  - **REST API**: Added DELETE `/api/vault/file?path=` and POST `/api/vault/rename` (from/to). Full API documented in `docs/api.md` covering 18 endpoints.
  - **Auth toggle**: `AUTH_ENABLED` env var (default true). Set `AUTH_ENABLED=false` for open development.
- Verification:
  - Server build clean (tsc)
  - Web build clean (138+ modules)
  - Playwright: login page renders, register flow works, vault loads after auth
- Risks or blockers: Per-user vault isolation not yet implemented (all users share same vault)
- Next recommended task: M7 — Plugin Runtime (manifest scanning, sandboxed plugin execution)
