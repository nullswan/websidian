# Lab Notes

Create one markdown file per meaningful iteration or session.

Purpose:

- preserve state between long-running sessions
- make handoffs cheap
- record why decisions were made
- prevent the loop from repeating dead ends

Filename convention:

`YYYY-MM-DD-HHMM-short-title.md`

Use `TEMPLATE.md` for structure.
